prefix=$PREFIX
exec_prefix=${prefix}
libdir=${prefix}/lib64
sharedlibdir=${libdir}
includedir=${prefix}/include

Name: igvm
Description: igvm library
Version: $VERSION

Requires:
Libs: -L${libdir} -L${sharedlibdir} -ligvm
Cflags: -I${includedir}
